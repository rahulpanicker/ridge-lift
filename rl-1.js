window.__RL=(window.__RL||'')+"\n(function () {\n  \"use strict\";\n\n  if (typeof THREE === \"undefined\") {\n    var fail = document.getElementById(\"fail\");\n    fail.style.display = \"flex\";\n    fail.textContent = \"Could not load Three.js from the CDN. Open this file with a network connection (file:// works) or serve it: python3 -m http.server\";\n    return;\n  }\n\n  var canvas = document.getElementById(\"c\");\n  var renderer;\n  try {\n    renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, powerPreference: \"high-performance\" });\n  } catch (e) {\n    document.getElementById(\"fail\").style.display = \"flex\";\n    document.getElementById(\"fail\").textContent = \"WebGL is required to fly Ridge Lift.\";\n    return;\n  }\n  renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));\n  renderer.setSize(window.innerWidth, window.innerHeight);\n  renderer.outputColorSpace = THREE.SRGBColorSpace;\n  renderer.toneMapping = THREE.ACESFilmicToneMapping;\n  renderer.toneMappingExposure = 1.08;\n  renderer.setClearColor(0x87a0b8, 1);\n\n  var scene = new THREE.Scene();\n  scene.fog = new THREE.FogExp2(0x9eb4c6, 0.000145);\n\n  var camera = new THREE.PerspectiveCamera(62, window.innerWidth / window.innerHeight, 0.4, 28000);\n  camera.position.set(-180, 520, 420);\n\n  var SUN_DIR = new THREE.Vector3(0.62, 0.58, 0.22).normalize();\n\n  var hemi = new THREE.HemisphereLight(0xb7d2ea, 0x6a5a40, 0.62);\n  scene.add(hemi);\n  var sun = new THREE.DirectionalLight(0xfff1d0, 1.55);\n  sun.position.copy(SUN_DIR.clone().multiplyScalar(4000));\n  scene.add(sun);\n  scene.add(new THREE.AmbientLight(0x6f8494, 0.22));\n\n  function hash2(ix, iz) {\n    var n = Math.sin(ix * 127.1 + iz * 311.7) * 43758.5453;\n    return n - Math.floor(n);\n  }\n  function smoothNoise(x, z) {\n    var x0 = Math.floor(x), z0 = Math.floor(z);\n    var fx = x - x0, fz = z - z0;\n    var u = fx * fx * (3 - 2 * fx);\n    var v = fz * fz * (3 - 2 * fz);\n    var a = hash2(x0, z0), b = hash2(x0 + 1, z0);\n    var c = hash2(x0, z0 + 1), d = hash2(x0 + 1, z0 + 1);\n    return a + (b - a) * u + (c - a) * v + (a - b - c + d) * u * v;\n  }\n  function fbm(x, z) {\n    var n = 0, a = 0.5, f = 1, s = 0;\n    for (var i = 0; i < 5; i++) {\n      n += a * (smoothNoise(x * f, z * f) * 2 - 1);\n      s += a;\n      a *= 0.5;\n      f *= 2.02;\n    }\n    return n / s;\n  }\n\n  function heightAt(x, z) {\n    var n1 = fbm(x * 0.00072, z * 0.00072);\n    var n2 = fbm(x * 0.00205 + 18, z * 0.00205);\n    var n3 = fbm(x * 0.006 + 90, z * 0.006);\n    var ridgeAmp = 430 + 70 * Math.sin(z * 0.00155) + 36 * n2;\n    var ridge = Math.exp(-Math.pow((x - 160) / 300, 2)) * ridgeAmp;\n    var ridge2 = Math.exp(-Math.pow((x - 1180) / 240, 2)) * (250 + 48 * Math.sin(z * 0.0021 + 0.7));\n    var west = Math.exp(-Math.pow((x + 980) / 420, 2)) * (150 + 40 * n2);\n    var dx1 = x - 210, dz1 = z - 860;\n    var peak1 = Math.exp(-(dx1 * dx1 + dz1 * dz1) / (190 * 190)) * 240;\n    var dx2 = x - 90, dz2 = z + 1520;\n    var peak2 = Math.exp(-(dx2 * dx2 + dz2 * dz2) / (220 * 220)) * 200;\n    var dx3 = x - 1280, dz3 = z - 200;\n    var peak3 = Math.exp(-(dx3 * dx3 + dz3 * dz3) / (260 * 260)) * 160;\n    var lakeA = Math.exp(-((x + 420) * (x + 420) + (z - 240) * (z - 240)) / (280 * 280));\n    var lakeB = Math.exp(-((x + 200) * (x + 200) + (z + 980) * (z + 980)) / (240 * 240));\n    var h = 36 + n1 * 58 + n3 * 10 + ridge + ridge2 * 0.9 + west * 0.72 + peak1 + peak2 + peak3;\n    h -= lakeA * 48 + lakeB * 40;\n    return Math.max(6, h);\n  }\n  function slopeX(x, z) {\n    var e = 8;\n    return (heightAt(x + e, z) - heightAt(x - e, z)) / (2 * e);\n  }\n  function slopeZ(x, z) {\n    var e = 8;\n    return (heightAt(x, z + e) - heightAt(x, z - e)) / (2 * e);\n  }\n\n  var skyGeo = new THREE.SphereGeometry(16000, 32, 24);\n  var skyMat = new THREE.ShaderMaterial({\n    side: THREE.BackSide,\n    depthWrite: false,\n    uniforms: {\n      topColor: { value: new THREE.Color(0x4e7eae) },\n      midColor: { value: new THREE.Color(0x9eb9d2) },\n      horColor: { value: new THREE.Color(0xd5c4a4) },\n      botColor: { value: new THREE.Color(0x7d8b7a) },\n      sunDir: { value: SUN_DIR.clone() }\n    },\n    vertexShader: [\n      \"varying vec3 vW;\",\n      \"void main(){\",\n      \"  vec4 w = modelMatrix * vec4(position,1.0);\",\n      \"  vW = w.xyz;\",\n      \"  gl_Position = projectionMatrix * modelViewMatrix * vec4(position,1.0);\",\n      \"}\"\n    ].join(\"\\n\"),\n    fragmentShader: [\n      \"varying vec3 vW;\",\n      \"uniform vec3 topColor, midColor, horColor, botColor, sunDir;\",\n      \"void main(){\",\n      \"  vec3 d = normalize(vW);\",\n      \"  float h = d.y;\",\n      \"  vec3 col = mix(horColor, midColor, smoothstep(0.0, 0.22, h));\",\n      \"  col = mix(col, topColor, smoothstep(0.12, 0.72, h));\",\n      \"  col = mix(botColor, col, smoothstep(-0.12, 0.04, h));\",\n      \"  float sun = pow(max(dot(d, sunDir), 0.0), 420.0);\",\n      \"  float glow = pow(max(dot(d, sunDir), 0.0), 12.0);\",\n      \"  float haze = pow(max(dot(d, sunDir), 0.0), 2.5);\",\n      \"  col += vec3(1.0, 0.92, 0.7) * sun * 2.4;\",\n      \"  col += vec3(1.0, 0.78, 0.42) * glow * 0.38;\",\n      \"  col += vec3(1.0, 0.7, 0.35) * haze * 0.12;\",\n      \"  gl_FragColor = vec4(col, 1.0);\",\n      \"}\"\n    ].join(\"\\n\")\n  });\n  var sky = new THREE.Mesh(skyGeo, skyMat);\n  scene.add(sky);\n\n  var sunMesh = new THREE.Mesh(\n    new THREE.SphereGeometry(90, 16, 16),\n    new THREE.MeshBasicMaterial({ color: 0xfff3c8, fog: false })\n  );\n  sunMesh.position.copy(SUN_DIR.clone().multiplyScalar(9000));\n  scene.add(sunMesh);\n\n  var WORLD = 6400;\n  var SEG = 192;\n  var tGeo = new THREE.PlaneGeometry(WORLD, WORLD, SEG, SEG);\n  tGeo.rotateX(-Math.PI / 2);\n  var tPos = tGeo.attributes.position;\n  var i;\n  for (i = 0; i < tPos.count; i++) {\n    tPos.setY(i, heightAt(tPos.getX(i), tPos.getZ(i)));\n  }\n  tGeo.computeVertexNormals();\n  var tN = tGeo.attributes.normal;\n  var colors = new Float32Array(tPos.count * 3);\n  var cTmp = new THREE.Color();\n  var grass = new THREE.Color(0x5a7a3e);\n  var meadow = new THREE.Color(0x7a8a4a);\n  var alpine = new THREE.Color(0x8a8a62);\n  var rock = new THREE.Color(0x6d6458);\n  var darkRock = new THREE.Color(0x4a433c);\n  var snow = new THREE.Color(0xeef2f4);\n  var dirt = new THREE.Color(0x6b5a40);\n  for (i = 0; i < tPos.count; i++) {\n    var y = tPos.getY(i);\n    var ny = tN.getY(i);\n    var steep = THREE.MathUtils.clamp(1 - ny * ny, 0, 1);\n    var nCol = fbm(tPos.getX(i) * 0.01, tPos.getZ(i) * 0.01);\n    cTmp.copy(grass).lerp(meadow, THREE.MathUtils.clamp((y - 40) / 140 + nCol * 0.25, 0, 1));\n    cTmp.lerp(alpine, THREE.MathUtils.clamp((y - 220) / 140, 0, 1));\n    cTmp.lerp(dirt, THREE.MathUtils.clamp((y - 20) / 80 * 0.15, 0, 0.25));\n    var rockMix = THREE.MathUtils.clamp(steep * 1.55 + (y - 260) / 280, 0, 1);\n    cTmp.lerp(rock, rockMix * 0.85);\n    if (steep > 0.45) cTmp.lerp(darkRock, (steep - 0.45) * 1.2);\n    var snowH = THREE.MathUtils.clamp((y - 390) / 90, 0, 1) * (1 - steep * 0.65);\n    cTmp.lerp(snow, snowH);\n    if (y < 26) cTmp.lerp(new THREE.Color(0x3d5340), 0.35);\n    colors[i * 3] = cTmp.r;\n    colors[i * 3 + 1] = cTmp.g;\n    colors[i * 3 + 2] = cTmp.b;\n  }\n  tGeo.setAttribute(\"color\", new THREE.BufferAttribute(colors, 3));\n  var terrain = new THREE.Mesh(\n    tGeo,\n    new THREE.MeshLambertMaterial({ vertexColors: true, fog: true })\n  );\n  terrain.matrixAutoUpdate = false;\n  terrain.updateMatrix();\n  scene.add(terrain);\n\n  var water = new THREE.Mesh(\n    new THREE.PlaneGeometry(WORLD, WORLD),\n    new THREE.MeshPhongMaterial({\n      color: 0x24586a,\n      transparent: true,\n      opacity: 0.62,\n      shininess: 90,\n      specular: 0x88c4d8,\n      fog: true\n    })\n  );\n  water.rotation.x = -Math.PI / 2;\n  water.position.y = 22;\n  scene.add(water);\n\n  var peakMat = new THREE.MeshLambertMaterial({ color: 0x8f9aa6, fog: true });\n  function addPeak(x, z, r, h) {\n    var m = new THREE.Mesh(new THREE.ConeGeometry(r, h, 7), peakMat);\n    m.position.set(x, heightAt(x, z) * 0.15 + h * 0.28, z);\n    m.rotation.y = Math.random() * 6;\n    scene.add(m);\n  }\n  addPeak(-2200, 2400, 480, 900);\n  addPeak(2600, 2500, 520, 980);\n  addPeak(-2400, -2200, 400, 760);\n  addPeak(2700, -2100, 560, 1100);\n\n  var cloudMat = new THREE.MeshLambertMaterial({\n    color: 0xf4f6f8,\n    transparent: true,\n    opacity: 0.34,\n    depthWrite: false,\n    fog: true\n  });\n  var clouds = new THREE.Group();\n  scene.add(clouds);\n  var cg = new THREE.SphereGeometry(1, 8, 6);\n  for (i = 0; i < 26; i++) {\n    var cl = new THREE.Group();\n    var bits = 4 + (i % 4);\n    var b;\n    for (b = 0; b < bits; b++) {\n      var s = new THREE.Mesh(cg, cloudMat);\n      var rs = 48 + Math.random() * 90;\n      s.scale.set(rs, rs * (0.38 + Math.random() * 0.22), rs * (0.7 + Math.random() * 0.4));\n      s.position.set((Math.random() - 0.5) * 140, (Math.random() - 0.5) * 24, (Math.random() - 0.5) * 90);\n      cl.add(s);\n    }\n    cl.position.set((Math.random() - 0.5) * 5200, 720 + Math.random() * 380, (Math.random() - 0.5) * 5200);\n    cl.userData.drift = 1.6 + Math.random() * 2.2;\n    clouds.add(cl);\n  }\n\n  function makeAircraft() {\n    var g = new THREE.Group();\n    var white = new THREE.MeshPhongMaterial({ color: 0xf3eee6, shininess: 38, specular: 0x444444 });\n    var orange = new THREE.MeshPhongMaterial({ color: 0xd4541a, shininess: 32, specular: 0x333333 });\n    var dark = new THREE.MeshPhongMaterial({ color: 0x1c2430, shininess: 50, specular: 0x555555 });\n    var glass = new THREE.MeshPhongMaterial({\n      color: 0x152030, shininess: 90, specular: 0x88aacc,\n      transparent: true, opacity: 0.55\n    });\n    var stripe = new THREE.MeshPhongMaterial({ color: 0xc43b1a, shininess: 26, specular: 0x222222 });\n    var fuse = new THREE.Mesh(new THREE.CapsuleGeometry(0.34, 3.9, 6, 12), white);\n    fuse.rotation.x = Math.PI / 2;\n    fuse.position.z = -0.15;\n    g.add(fuse);\n    var nose = new THREE.Mesh(new THREE.SphereGeometry(0.33, 12, 10), white);\n    nose.scale.set(1, 0.92, 1.15);\n    nose.position.z = 2.05;\n    g.add(nose);\n    var spinner = new THREE.Mesh(new THREE.SphereGeometry(0.16, 10, 8), orange);\n    spinner.scale.set(1, 1, 1.3);\n    spinner.position.z = 2.42;\n    g.add(spinner);\n    var canopy = new THREE.Mesh(new THREE.SphereGeometry(0.4, 12, 10), glass);\n    canopy.scale.set(0.72, 0.62, 1.15);\n    canopy.position.set(0, 0.28, 0.55);\n    g.add(canopy);\n    var band = new THREE.Mesh(new THREE.BoxGeometry(0.72, 0.08, 2.4), stripe);\n    band.position.set(0, 0.02, -0.4);\n    g.add(band);\n    function wingHalf(side) {\n      var w = new THREE.Group();\n      var panel = new THREE.Mesh(new THREE.BoxGeometry(7.6, 0.09, 1.28), white);\n      panel.position.x = side * 4.15;\n      panel.rotation.z = side * 0.06;\n      w.add(panel);\n      var root = new THREE.Mesh(new THREE.BoxGeometry(1.4, 0.16, 1.42), white);\n      root.position.x = side * 0.85;\n      w.add(root);\n      var tip = new THREE.Mesh(new THREE.BoxGeometry(0.55, 0.1, 1.15), orange);\n      tip.position.set(side * 7.85, 0.18, 0);\n      tip.rotation.z = side * 0.18;\n      w.add(tip);\n      var le = new THREE.Mesh(new THREE.BoxGeometry(7.4, 0.04, 0.12), stripe);\n      le.position.set(side * 4.1, 0.03, 0.6);\n      le.rotation.z = side * 0.06;\n      w.add(le);\n      return w;\n    }\n    g.add(wingHalf(1));\n    g.add(wingHalf(-1));\n    var ht = new THREE.Mesh(new THREE.BoxGeometry(3.1, 0.07, 0.72), white);\n    ht.position.set(0, 0.08, -2.15);\n    g.add(ht);\n    var vt = new THREE.Mesh(new THREE.BoxGeometry(0.08, 1.12, 0.78), white);\n    vt.position.set(0, 0.62, -2.15);\n    g.add(vt);\n    var rudderTip = new THREE.Mesh(new THREE.BoxGeometry(0.09, 0.28, 0.4), orange);\n    rudderTip.position.set(0, 1.12, -2.28);\n    g.add(rudderTip);\n    var skid = new THREE.Mesh(new THREE.BoxGeometry(0.14, 0.1, 1.1), dark);\n    skid.position.set(0, -0.4, 0.3);\n    g.add(skid);\n    var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.16, 0.16, 0.1, 12), dark);\n    wheel.rotation.z = Math.PI / 2;\n    wheel.position.set(0, -0.48, 0.15);\n    g.add(wheel);\n    var prop = new THREE.Group();\n    var bladeMat = new THREE.MeshPhongMaterial({\n      color: 0x222830, shininess: 40, specular: 0x333333, transparent: true, opacity: 0.85\n    });\n    var blade = new THREE.Mesh(new THREE.BoxGeometry(0.08, 1.55, 0.14), bladeMat);\n    var blade2 = blade.clone();\n    blade2.rotation.z = Math.PI / 2;\n    prop.add(blade);\n    prop.add(blade2);\n    prop.position.z = 2.48;\n    g.add(prop);\n    g.userData.prop = prop;\n    var disc = new THREE.Mesh(\n      new THREE.CircleGeometry(0.82, 20),\n      new THREE.MeshBasicMaterial({\n        color: 0xcfd6dc, transparent: true, opacity: 0.14, side: THREE.DoubleSide, depthWrite: false\n      })\n    );\n    disc.position.z = 2.48;\n    g.add(disc);\n    g.userData.disc = disc;\n    return g;\n  }\n\n  var aircraft = makeAircraft();\n  scene.add(aircraft);\n\n  var blobTex;\n  (function () {\n    var c2 = document.createElement(\"canvas\");\n    c2.width = 64; c2.height = 64;\n    var ctx = c2.getContext(\"2d\");\n    var grd = ctx.createRadialGradient(32, 32, 2, 32, 32, 30);\n    grd.addColorStop(0, \"rgba(0,0,0,0.45)\");\n    grd.addColorStop(1, \"rgba(0,0,0,0)\");\n    ctx.fillStyle = grd;\n    ctx.fillRect(0, 0, 64, 64);\n    blobTex = new THREE.CanvasTexture(c2);\n  })();\n  var blob = new THREE.Mesh(\n    new THREE.PlaneGeometry(10, 10),\n    new THREE.MeshBasicMaterial({ map: blobTex, transparent: true, depthWrite: false, opacity: 0.55 })\n  );\n  blob.rotation.x = -Math.PI / 2;\n  scene.add(blob);\n\n  var ringMat = new THREE.MeshPhongMaterial({\n    color: 0xe8a23a,\n    emissive: 0xe8a23a,\n    emissiveIntensity: 0.55,\n    shininess: 45,\n    specular: 0x664411,\n    transparent: true,\n    opacity: 0.85\n  });\n  var ringGeo = new THREE.TorusGeometry(16, 0.42, 8, 28);\n  var rings = [];\n  var ringZs = [-1400, -1050, -700, -360, 0, 360, 720, 1080, 1420, 1760];\n  for (i = 0; i < ringZs.length; i++) {\n    var rz = ringZs[i];\n    var rx = 70 + Math.sin(i * 1.3) * 18;\n    var ry = heightAt(rx, rz) + 42 + (i % 3) * 10;\n    var mesh = new THREE.Mesh(ringGeo, ringMat.clone());\n    mesh.position.set(rx, ry, rz);\n    mesh.rotation.y = (i % 2 === 0 ? 0.12 : -0.1);\n    scene.add(mesh);\n    rings.push({ mesh: mesh, taken: false, x: rx, y: ry, z: rz });\n  }\n\n  var thermals = [\n    { x: -280, z: 180, r: 95, s: 7.2, p: 0.0 },\n    { x: -120, z: -620, r: 120, s: 6.1, p: 1.2 },\n    { x: 40, z: 980, r: 88, s: 5.4, p: 2.4 },\n    { x: -480, z: 640, r: 140, s: 4.8, p: 0.7 },\n    { x: -60, z: -1280, r: 100, s: 6.6, ";
