window.__RL=(window.__RL||'')+"ThrUi();\n  }\n\n  (function bindTouchPads() {\n    var stickEl = document.getElementById(\"stick\");\n    var railEl = document.getElementById(\"thr-rail\");\n    function touchById(list, id) {\n      if (!list) return null;\n      for (var i = 0; i < list.length; i++) {\n        if (list[i].identifier === id) return list[i];\n      }\n      return null;\n    }\n    function firstTouch(e) {\n      return (e.changedTouches && e.changedTouches[0]) || (e.touches && e.touches[0]) || null;\n    }\n    stickEl.addEventListener(\"pointerdown\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      enableTouchUi();\n      stick.active = true;\n      stick.id = e.pointerId;\n      try { stickEl.setPointerCapture(e.pointerId); } catch (err) {}\n      applyStick(e.clientX, e.clientY);\n    });\n    stickEl.addEventListener(\"pointermove\", function (e) {\n      if (!stick.active) return;\n      if (e.pointerType !== \"touch\" && e.pointerId !== stick.id) return;\n      e.preventDefault();\n      applyStick(e.clientX, e.clientY);\n    });\n    function stickUp(e) {\n      if (e.pointerId !== stick.id) return;\n      endStick();\n    }\n    stickEl.addEventListener(\"pointerup\", stickUp);\n    // iOS fires pointercancel right after pointerdown — do not endStick().\n    stickEl.addEventListener(\"touchstart\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      enableTouchUi();\n      var t = firstTouch(e);\n      if (!t) return;\n      stick.active = true;\n      stick.id = t.identifier;\n      applyStick(t.clientX, t.clientY);\n    }, { passive: false });\n    stickEl.addEventListener(\"touchmove\", function (e) {\n      if (!stick.active) return;\n      e.preventDefault();\n      var t = touchById(e.touches, stick.id) || firstTouch(e);\n      if (t) applyStick(t.clientX, t.clientY);\n    }, { passive: false });\n    stickEl.addEventListener(\"touchend\", function (e) {\n      e.preventDefault();\n      var ended = !!touchById(e.changedTouches, stick.id);\n      if (ended || !e.touches.length) endStick();\n    }, { passive: false });\n    stickEl.addEventListener(\"touchcancel\", function (e) {\n      e.preventDefault();\n    }, { passive: false });\n    railEl.addEventListener(\"pointerdown\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      enableTouchUi();\n      thrPad.active = true;\n      thrPad.id = e.pointerId;\n      try { railEl.setPointerCapture(e.pointerId); } catch (err) {}\n      setThrFromY(e.clientY);\n    });\n    railEl.addEventListener(\"pointermove\", function (e) {\n      if (!thrPad.active) return;\n      if (e.pointerType !== \"touch\" && e.pointerId !== thrPad.id) return;\n      e.preventDefault();\n      setThrFromY(e.clientY);\n    });\n    function thrUp(e) {\n      if (e.pointerId !== thrPad.id) return;\n      thrPad.active = false;\n      thrPad.id = null;\n    }\n    railEl.addEventListener(\"pointerup\", thrUp);\n    railEl.addEventListener(\"pointercancel\", thrUp);\n    railEl.addEventListener(\"touchstart\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      enableTouchUi();\n      var t = firstTouch(e);\n      if (!t) return;\n      thrPad.active = true;\n      thrPad.id = t.identifier;\n      setThrFromY(t.clientY);\n    }, { passive: false });\n    railEl.addEventListener(\"touchmove\", function (e) {\n      if (!thrPad.active) return;\n      e.preventDefault();\n      var t = touchById(e.touches, thrPad.id) || firstTouch(e);\n      if (t) setThrFromY(t.clientY);\n    }, { passive: false });\n    railEl.addEventListener(\"touchend\", function (e) {\n      e.preventDefault();\n      var ended = !!touchById(e.changedTouches, thrPad.id);\n      if (ended || !e.touches.length) {\n        thrPad.active = false;\n        thrPad.id = null;\n      }\n    }, { passive: false });\n    railEl.addEventListener(\"touchcancel\", function (e) {\n      e.preventDefault();\n    }, { passive: false });\n  })();\n\n  document.getElementById(\"touch-relaunch\").addEventListener(\"click\", function (e) {\n    e.preventDefault();\n    e.stopPropagation();\n    if (phase === \"fly\" || phase === \"crash\" || phase === \"landed\" || phase === \"menu\") startFlight();\n  });\n  document.getElementById(\"touch-cam\").addEventListener(\"click\", function (e) {\n    e.preventDefault();\n    e.stopPropagation();\n    if (phase === \"fly\") cycleCam();\n  });\n  document.getElementById(\"touch-mute\").addEventListener(\"click\", function (e) {\n    e.preventDefault();\n    e.stopPropagation();\n    toggleMute();\n    document.getElementById(\"touch-mute\").textContent = audio.muted ? \"Unmute\" : \"Mute\";\n  });\n  document.getElementById(\"levelBtn\").addEventListener(\"click\", function (e) {\n    e.preventDefault();\n    e.stopPropagation();\n    calibrateTilt();\n    toast(\"Level set\");\n  });\n\n  window.addEventListener(\"touchstart\", function () { enableTouchUi(); }, { passive: true });\n  document.addEventListener(\"touchmove\", function (e) {\n    e.preventDefault();\n    if (stick.active && e.touches && e.touches.length) {\n      var t = null;\n      for (var i = 0; i < e.touches.length; i++) {\n        if (e.touches[i].identifier === stick.id) { t = e.touches[i]; break; }\n      }\n      if (!t) t = e.touches[0];\n      applyStick(t.clientX, t.clientY);\n    }\n  }, { passive: false });\n  document.addEventListener(\"gesturestart\", function (e) { e.preventDefault(); });\n  document.addEventListener(\"gesturechange\", function (e) { e.preventDefault(); });\n  document.addEventListener(\"gestureend\", function (e) { e.preventDefault(); });\n  window.addEventListener(\"orientationchange\", function () { if (tilt.granted) calibrateTilt(); });\n  if (screen.orientation && screen.orientation.addEventListener) {\n    screen.orientation.addEventListener(\"change\", function () { if (tilt.granted) calibrateTilt(); });\n  }\n  var mouseLook = { x: 0, y: 0, tx: 0, ty: 0, down: false };\n  window.addEventListener(\"keydown\", function (e) {\n    keys[e.code] = true;\n    if ([\"Space\", \"ArrowUp\", \"ArrowDown\", \"ArrowLeft\", \"ArrowRight\"].indexOf(e.code) >= 0) e.preventDefault();\n    if (phase === \"menu\") startFlight();\n    else if ((phase === \"crash\" || phase === \"landed\") && e.code === \"Space\") startFlight();\n    if (e.code === \"KeyC\" && (phase === \"fly\")) cycleCam();\n    if (e.code === \"KeyM\") { toggleMute(); var mb = document.getElementById(\"touch-mute\"); if (mb) mb.textContent = audio.muted ? \"Unmute\" : \"Mute\"; }\n    if (e.code === \"Space\" && phase === \"fly\") startFlight();\n  });\n  window.addEventListener(\"keyup\", function (e) { keys[e.code] = false; });\n  window.addEventListener(\"blur\", function () { keys = Object.create(null); });\n\n  canvas.addEventListener(\"mousedown\", function (e) {\n    if (e.button === 2 || e.button === 0) mouseLook.down = true;\n  });\n  window.addEventListener(\"mouseup\", function () { mouseLook.down = false; });\n  window.addEventListener(\"mousemove\", function (e) {\n    if (!mouseLook.down || phase !== \"fly\") return;\n    mouseLook.tx += e.movementX * 0.0022;\n    mouseLook.ty = THREE.MathUtils.clamp(mouseLook.ty + e.movementY * 0.002, -0.5, 0.5);\n  });\n  canvas.addEventListener(\"contextmenu\", function (e) { e.preventDefault(); });\n\n  function readInput(dt) {\n    var p = 0, r = 0, y = 0;\n    if (keys.KeyW || keys.ArrowUp) p += 1;\n    if (keys.KeyS || keys.ArrowDown) p -= 1;\n    if (keys.KeyA || keys.ArrowLeft) r += 1;\n    if (keys.KeyD || keys.ArrowRight) r -= 1;\n    if (keys.KeyQ) y += 1;\n    if (keys.KeyE) y -= 1;\n    var mp = 0, mr = 0;\n    if (stick.active) {\n      mp = stick.pitch;\n      mr = stick.roll;\n    } else if (tilt.live && tilt.granted) {\n      mp = tilt.pitch;\n      mr = tilt.roll;\n    }\n    p = THREE.MathUtils.clamp(p + mp, -1, 1);\n    r = THREE.MathUtils.clamp(r + mr, -1, 1);\n    input.pitch += (p - input.pitch) * Math.min(1, dt * 8);\n    input.roll += (r - input.roll) * Math.min(1, dt * 8);\n    input.yaw += (y - input.yaw) * Math.min(1, dt * 8);\n    var thr = 0;\n    if (keys.ShiftLeft || keys.ShiftRight || keys.Equal || keys.NumpadAdd) thr += 1;\n    if (keys.ControlLeft || keys.ControlRight || keys.Minus || keys.NumpadSubtract) thr -= 1;\n    if (!thrPad.active && thr !== 0) {\n      ac.throttle = THREE.MathUtils.clamp(ac.throttle + thr * dt * 0.55, 0, 1);\n    }\n    updateThrUi();\n  }\n\n  var camMode = 0;\n  var camPos = new THREE.Vector3(-120, 480, 380);\n  var camLook = new THREE.Vector3(160, 360, 0);\n  var shake = 0;\n  function cycleCam() {\n    camMode = (camMode + 1) % 3;\n    toast([\"Chase\", \"Canopy\", \"Cinematic\"][camMode]);\n  }\n  function updateCamera(dt, cinematic) {\n    bodyAxes();\n    if (cinematic) {\n      var t = performance.now() * 0.00008;\n      var r = 980;\n      camPos.set(Math.cos(t) * r + 80, 420 + Math.sin(t * 0.7) * 60, Math.sin(t) * r);\n      camLook.set(160, 340, Math.sin(t * 0.5) * 200);\n    } else {\n      var back, up, side;\n      if (camMode === 0) { back = 19; up = 4.4; }\n      else if (camMode === 1) { back = 7.2; up = 1.55; }\n      else { back = 36; up = 9; }\n      mouseLook.x += (mouseLook.tx - mouseLook.x) * 0.12;\n      mouseLook.y += (mouseLook.ty - mouseLook.y) * 0.12;\n      if (!mouseLook.down) {\n        mouseLook.tx *= 0.9;\n        mouseLook.ty *= 0.9;\n      }\n      _tmp.copy(ac.pos);\n      _tmp.addScaledVector(_fwd, -back);\n      _tmp.addScaledVector(_up, up);\n      _tmp.addScaledVector(_right, mouseLook.x * 10);\n      _tmp.y += mouseLook.y * 8;\n      var floor = heightAt(_tmp.x, _tmp.z) + 2.2;\n      if (_tmp.y < floor) _tmp.y = floor;\n      camPos.lerp(_tmp, 1 - Math.pow(0.0018, dt));\n      camLook.copy(ac.pos).addScaledVector(_fwd, camMode === 1 ? 18 : 14).addScaledVector(_up, 0.8);\n    }\n    if (shake > 0) {\n      camPos.x += (Math.random() - 0.5) * shake;\n      camPos.y += (Math.random() - 0.5) * shake * 0.6;\n      shake *= Math.pow(0.08, dt);\n    }\n    camera.position.copy(camPos);\n    camera.lookAt(camLook);\n  }\n\n  var phase = \"menu\";\n  var flown = 0;\n  var airTime = 0;\n  var pathDist = 0;\n  var ringsHit = 0;\n  var lastPos = new THREE.Vector3();\n  var toastT = 0;\n  var best = { time: 0, dist: 0 };\n  try {\n    var saved = JSON.parse(localStorage.getItem(\"ridgeLiftBest\") || \"{}\");\n    if (saved.time) best.time = saved.time;\n    if (saved.dist) best.dist = saved.dist;\n  } catch (e) {}\n\n  function fmtTime(s) {\n    s = Math.max(0, s);\n    var m = Math.floor(s / 60);\n    var sec = Math.floor(s % 60);\n    return m + \":\" + (sec < 10 ? \"0\" : \"\") + sec;\n  }\n  function persistBest() {\n    if (airTime > best.time) best.time = airTime;\n    if (pathDist > best.dist) best.dist = pathDist;\n    try { localStorage.setItem(\"ridgeLiftBest\", JSON.stringify(best)); } catch (e) {}\n    document.getElementById(\"bestTime\").textContent = fmtTime(best.time);\n    document.getElementById(\"bestDist\").textContent = (best.dist / 1000).toFixed(1) + \" km\";\n  }\n  persistBest();\n\n  function toast(msg) {\n    var el = document.getElementById(\"toast\");\n    el.textContent = msg;\n    el.classList.add(\"show\");\n    toastT = 1.8;\n  }\n  function toggleMute() {\n    audio.muted = !audio.muted;\n    document.getElementById(\"mutebadge\").classList.toggle(\"on\", audio.muted);\n    toast(audio.muted ? \"Muted\" : \"Audio on\");\n  }\n\n  function setGauge(id, cls) {\n    var el = document.getElementById(id);\n    el.classList.remove(\"warn\", \"bad\", \"lift\", \"sink\");\n    if (cls) el.classList.add(cls);\n  }\n\n  function updateHUD() {\n    var kt = ac.ias * 1.94384;\n    document.getElementById(\"ias\").textContent = Math.round(kt) + \" / \" + ac.ias.toFixed(0);\n    setGauge(\"gIas\", ac.ias < 22 ? \"warn\" : (ac.ias < 18 ? \"bad\" : \"\"));\n    var ft = ac.pos.y * 3.28084;\n    document.getElementById(\"alt\").textContent = Math.round(ft) + \" / \" + Math.round(ac.pos.y);\n    document.getElementById(\"hdg\").textContent = String(Math.round(ac.heading)).padStart(3, \"0\");\n    var fpm = ac.vs * 196.85;\n    var sign = fpm >= 0 ? \"+\" : \"\";\n    document.getElementById(\"vario\").textContent = sign + Math.round(fpm) + \" / \" + (ac.vs >= 0 ? \"+\" : \"\") + ac.vs.toFixed(1);\n    setGauge(\"gVar\", ac.vs > 0.6 ? \"lift\" : (ac.vs < -2.5 ? \"sink\" : \"\"));\n    document.getElementById(\"thr\").textContent = Math.round(ac.throttle * 100) + \"%\";\n    var aDeg = ac.alpha / DEG;\n    document.getElementById(\"aoa\").textContent = (aDeg >= 0 ? \"+\" : \"\") + aDeg.toFixed(1);\n    setGauge(\"gAoa\", ac.buffet > 0.7 ? \"bad\" : (ac.buffet > 0.25 ? \"warn\" : \"\"));\n    document.getElementById(\"time\").textContent = fmtTime(airTime);\n    document.getElementById(\"dist\").textContent = (pathDist / 1000).toFixed(1);\n    document.getElementById(\"rings\").textContent = String(ringsHit);\n    document.getElementById(\"stallFlag\").classList.toggle(\"on\", ac.buffet > 0.62);\n  }\n\n  function startFlight() {\n    ensureAudio();\n    if (audio.ctx && audio.ctx.state === \"suspended\") audio.ctx.resume();\n    requestTiltPermission();\n    calibrateTilt();\n    launch();\n    updateThrUi();\n    lastPos.copy(ac.pos);\n    flown = 0;\n    airTime = 0;\n    pathDist = 0;\n    ringsHit = 0;\n    shake = 0;\n    phase = \"fly\";\n    document.getElementById(\"start\").classList.add(\"hide\");\n    document.getElementById(\"crash\").classList.remove(\"show\");\n    document.getElementById(\"hud\").classList.add(\"on\");\n    camPos.copy(ac.pos).add(new THREE.Vector3(-16, 6, -8));\n    camLook.copy(ac.pos);\n    syncPhaseClass();\n    toast(\"Airborne\");\n  }\n\n  function endFlight(kind) {\n    phase = kind;\n    persistBest();\n    shake = 1.4;\n    document.getElementById(\"crashKicker\").textContent = kind === \"landed\" ? \"Landed\" : \"Flight ended\";\n    document.getElementById(\"crashTitle\").textContent = kind === \"landed\" ? \"Down safe.\" : \"Down.\";\n    document.getElementById(\"crashTag\").textContent = kind === \"landed\"\n      ? \"You traded the last of your energy for the meadow.\"\n      : \"The mountain keeps what you give it.\";\n    document.getElementById(\"cTime\").textContent = fmtTime(airTime);\n    document.getElementById(\"cDist\").textContent = (pathDist / 1000).toFixed(2) + \" km\";\n    document.getElementById(\"cRings\").textContent = String(ringsHit);\n    document.getElementById(\"crash\").classList.add(\"show\");\n    document.getElementById(\"stallFlag\").classList.remove(\"on\");\n    endStick();\n    syncPhaseClass();\n  }\n\n  function beginFromUser(e) {\n    if (e) e.stopPropagation();\n    ensureAudio();\n    requestTiltPermission();\n    startFlight();\n  }\n  document.getElementById(\"flyBtn\").addEventListener(\"click\", beginFromUser);\n  document.getElementById(\"reBtn\").addEventListener(\"click\", beginFromUser);\n  document.getElementById(\"start\").addEventListener(\"click\", beginFromUser);\n  document.getElementById(\"crash\").addEventListener(\"click\", beginFromUser);\n\n  var clock = new THREE.Clock();\n  var simTime = 0;\n\n  function checkRings() {\n    var r, d, dx, dy, dz;\n    for (r = 0; r < rings.length; r++) {\n      if (rings[r].taken) continue;\n      dx = ac.pos.x - rings[r].x;\n      dy = ac.pos.y - rings[r].y;\n      dz = ac.pos.z - rings[r].z;\n      d = Math.sqrt(dx * dx + dy * dy + dz * dz);\n      if (d < 17.5) {\n        rings[r].taken = true;\n        rings[r].mesh.material.emissiveIntensity = 1.8;\n        rings[r].mesh.material.opacity = 0.25;\n        ringsHit += 1;\n        toast(\"Gate \" + ringsHit + \" / \" + rings.length);\n      }\n    }\n  }\n\n  function syncAircraft(dt) {\n    aircraft.position.copy(ac.pos);\n    aircraft.quaternion.copy(ac.q);\n    var spin = 8 + ac.throttle * 42 + ac.ias * 0.15;\n    aircraft.userData.prop.rotation.z += dt * spin;\n    aircraft.userData.disc.material.opacity = 0.04 + ac.throttle * 0.22;\n    var gh = heightAt(ac.pos.x, ac.pos.z);\n    blob.position.set(ac.pos.x, gh + 0.15, ac.pos.z);\n    var agl = ac.pos.y - gh;\n    blob.material.opacity = THREE.MathUtils.clamp(0.55 * (1 - agl / 80), 0.02, 0.5);\n    var sc = 8 + agl * 0.12;\n    blob.scale.set(sc, sc, 1);\n  }\n\n  function tick() {\n    requestAnimationFrame(tick);\n    var dt = clock.getDelta();\n    if (dt > 0.05) dt = 0.05;\n    simTime += dt;\n    if (toastT > 0) {\n      toastT -= dt;\n      if (toastT <= 0) document.getElementById(\"toast\").classList.remove(\"show\");\n    }\n    var c;\n    for (c = 0; c < clouds.children.length; c++) {\n      clouds.children[c].position.x += clouds.children[c].userData.drift * dt;\n      if (clouds.children[c].position.x > 3000) clouds.children[c].position.x = -3000;\n    }\n    var rg;\n    for (rg = 0; rg < rings.length; rg++) {\n      rings[rg].mesh.rotation.z += dt * 0.35;\n    }\n    if (phase === \"fly\") {\n      readInput(dt);\n      var st = stepPhysics(dt, input, simTime);\n      pathDist += ac.pos.distanceTo(lastPos);\n      lastPos.copy(ac.pos);\n      airTime += dt;\n      checkRings();\n      if (ac.buffet > 0.4) shake = Math.max(shake, ac.buffet * 0.35);\n      if (st === \"crash\" || st === \"landed\") endFlight(st);\n    } else if (phase === \"menu\") {\n      ac.pos.set(80, heightAt(80, -40) + 14, -40);\n    }\n    syncAircraft(dt);\n    updateCamera(dt, phase === \"menu\");\n    if (phase === \"fly\") updateHUD();\n    updateAudio(dt, phase === \"fly\");\n    sky.position.copy(camera.position);\n    renderer.render(scene, camera);\n  }\n\n  window.addEventListener(\"resize\", function () {\n    camera.aspect = window.innerWidth / window.innerHeight;\n    camera.updateProjectionMatrix();\n    renderer.setSize(window.innerWidth, window.innerHeight);\n    syncPhaseClass();\n    updateThrUi();\n  });\n\n  launch();\n  lastPos.copy(ac.pos);\n  syncPhaseClass();\n  updateThrUi();\n  tick();\n})();\n\n";
